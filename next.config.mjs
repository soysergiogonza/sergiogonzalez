/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "sergiogonzalez.tech", 'plus.unsplash.com'],
  },
};

export default nextConfig;
