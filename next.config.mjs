/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "sergiogonzalez.tech", 'images.unsplash.com', "plus.unsplash.com", 'your-domain.com'],
  },
};

export default nextConfig;
